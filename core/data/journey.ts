import journeyJson from "./json/site/journey.json";

/** 여정 요약 + 타임라인 — 원본: `core/data/json/site/journey.json` */
export const journeySummary = journeyJson.summary;
export const journey = journeyJson.timeline;
